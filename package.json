{
  "name": "profile_website",
  "version": "1.0.0",
  "description": "Presentational website project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/three": "^0.175.0",
    "concurrently": "^9.1.2",
    "vite": "^6.2.6"
  },
  "dependencies": {
    "three": "^0.175.0"
  }
}
